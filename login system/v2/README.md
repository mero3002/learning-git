## welcome to version 0.2 where things get a little more interesting,
- Added a menu.
- Added a warm goodbye message.

version 0.3 should contain:
- Ask the user to re-enter only the password if it's wrong.
- Keep one data source.
- Completely empty JSON file error handling.

and i don't promise to create a 0.3 version..
