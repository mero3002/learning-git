### New Featuers Added:
- Ask the user to re-enter only the password if it's wrong.
- Separate 'readUsername' and 'doesUserExist' functions instead of 'readAndValidate' for better readability and optimization.
- Keep one data source.
- Check if the database is already empty before clearing.
- Handle a corrupted/empty JSON file.
- Add modifying account info function.

yes, i lied twice, for one i created a 0.3 version, for second i added even more features to it.

v0.4 should have:
idk for now
